/*
 * program to read data into
 * a two-dimensional array
 */
#include <stdio.h>
#define MAXSIZE 50


int main(void) {
	int grade[MAXSIZE][4];
	int class_size;
	int stnum;

	printf("How many students are in the class?> ");
	scanf("%d", &class_size);
}